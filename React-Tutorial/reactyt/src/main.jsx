import React from 'react';
import ReactDOM from 'react-dom/client';
import Home from './Home';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <Home />
  </React.StrictMode>
);
// This code imports React and ReactDOM, and renders the Home component into the root element of the HTML document.
